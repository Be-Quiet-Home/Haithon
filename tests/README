This directory contains tests for Haithon.

The suite currently combines historical interactive probes with focused
regression tests. Regression tests are standalone Python programs and must exit
with a nonzero status when their contract is violated.
